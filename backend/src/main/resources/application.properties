server.port=8080
# Path to model metrics JSON (from train_rf.py). Defaults assume running from backend/ (e.g. mvn spring-boot:run).
model.metrics-path=./models/metrics.json
# Python predict script (run from backend dir: python ml/model_store.py)
model.python-working-dir=.
model.python-script=ml/model_store.py
