# Backend environment (optional). Spring Boot reads these when set.
# To use: export vars before mvn spring-boot:run, or set in your IDE run config.

# Server
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# Option A: Run from backend/ (cd backend && mvn spring-boot:run)
# Defaults in application.properties already work; no need to set these.
# -----------------------------------------------------------------------------
# MODEL_METRICS_PATH=./models/metrics.json
# MODEL_PYTHON_WORKING_DIR=.
# MODEL_PYTHON_SCRIPT=ml/model_store.py

# -----------------------------------------------------------------------------
# Option B: Run from repo root (mvn spring-boot:run -f backend/pom.xml)
# Uncomment and use these so model and script paths resolve.
# -----------------------------------------------------------------------------
# SERVER_PORT=8080
# MODEL_METRICS_PATH=./backend/models/metrics.json
# MODEL_PYTHON_SCRIPT=backend/ml/model_store.py
# MODEL_PYTHON_WORKING_DIR=.
