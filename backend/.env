# Backend: run from backend/ (cd backend && mvn spring-boot:run).
# Export before starting, or set in IDE: Run > Edit Configurations > Environment variables.
# Codespaces: frontend is at https://scaling-potato-pjp55vgw5jxgf774q-5173.github.dev
SERVER_PORT=8080
MODEL_METRICS_PATH=./models/metrics.json
MODEL_PYTHON_WORKING_DIR=.
MODEL_PYTHON_SCRIPT=ml/model_store.py
