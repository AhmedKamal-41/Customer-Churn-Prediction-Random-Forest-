# Frontend (Vite). Copy to .env and adjust. Restart dev server after changes.

# Backend API base URL (used by churnApi, modelApi, client).
# Leave unset in local dev: requests use relative /api/* and Vite proxies to http://localhost:8080 (no CORS).
# Set for Codespaces: use the URL from the Ports panel for port 8080 (domain is preview.app.github.dev, not github.dev)
# e.g. VITE_API_URL=https://your-codespace-name-8080.preview.app.github.dev
# VITE_API_URL=

# Optional: force offline mode (1 = always show offline, 0 = use real API)
# VITE_FORCE_OFFLINE=0

# Optional: override URL for automation summary JSON (e.g. GitHub Pages). Default: /qa/automation-summary.json
# VITE_AUTOMATION_SUMMARY_URL=
